---
import AnimatedCTAButton from './AnimatedCTAButton.astro';
import Card from './Card.astro';

interface Props {
  data: {
    title: string;
    subtitle: string;
    mainTitle: string;
    description: string;
    ctaText: string;
    benefits: string[];
    footer: string;
  };
}

const { data } = Astro.props;
---

<section id="cta" class="relative py-12 md:py-20">
  <div class="mx-auto max-w-4xl px-6">
    <div class="mx-auto max-w-2xl text-center mb-8 md:mb-12">
      <h2 class="text-balance text-3xl font-bold md:text-4xl">{data.title}</h2>
    </div>
    <div>
      <Card class="relative rounded-3xl border shadow-lg bg-white">
        <div class="grid items-center gap-8 divide-y p-8 md:grid-cols-2 md:divide-x md:divide-y-0">
          <div class="pb-8 text-center md:pb-0 md:pr-8">
            <h3 class="text-xl font-semibold text-black">{data.subtitle}</h3>
            
            <div class="my-6">
              <h4 class="text-2xl font-bold text-blue-600 mb-3">{data.mainTitle}</h4>
              <p class="text-black">
                {data.description}
              </p>
            </div>

            <div class="flex justify-center">
              <AnimatedCTAButton href="https://cal.com/ramon-melchior/15min">
                {data.ctaText}
              </AnimatedCTAButton>
            </div>

            <p class="text-gray-600 mt-6 text-sm">{data.footer}</p>
          </div>
          <div class="relative">
            <ul role="list" class="space-y-3">
              {data.benefits.map((item) => (
                <li class="flex items-center gap-3">
                  <div class="flex-shrink-0 w-5 h-5 bg-blue-600 rounded-full flex items-center justify-center">
                    <svg class="size-3 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                    </svg>
                  </div>
                  <span class="text-sm font-medium text-black">{item}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </Card>
    </div>
  </div>
</section>